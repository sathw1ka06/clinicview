import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFF7F5F0); 
  static const Color surface = Colors.white;
  
  // Accents, Buttons, & Status
  static const Color primaryButton = Color(0xFF2C2C2C); 
  static const Color accentYellow = Color(0xFFFFC107); 
  static const Color success = Color(0xFF388E3C); // <-- Added this back!
  static const Color error = Color(0xFFD32F2F);   // <-- Added this back!
  
  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  
  // Borders
  static const Color borderLight = Color(0xFFE0E0E0);
  static const Color focusBorder = Color(0xFFFFC107); 
}